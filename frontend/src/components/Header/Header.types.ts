import { IStyle } from "@fluentui/react";

type HeaderStyles = {
  root: IStyle;
  menuButtons: IStyle;
  menuButton: IStyle;
  menuButtonText: IStyle;
  menuButtonSelected: IStyle;
  logo: IStyle;
  navButton: IStyle;
};

export type { HeaderStyles };
